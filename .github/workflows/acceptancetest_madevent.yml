# This is a basic workflow to help you get started with Actions

name: running acceptance test (madevent)
# Controls when the workflow will run
#    branches: [ main LTS ]
on:
  # Triggers the workflow on push or pull request events but only for the 3.4.0 branch
  push:
    # All branches, no tags: a release tag points at a commit that already
    # ran this suite when it was pushed to main, and release.yml gates on
    # that main run rather than re-testing the same tree. '**' is needed
    # over '*' so branch names containing a slash still match.
    branches:
      - '**'
    paths-ignore:
      - 'docs/**'
#      - '.github/**'

  pull_request:
    types: [opened, synchronize, reopened]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:
    inputs:
      message:
        description: 'running acceptance test'
        required: true

env:
  commitmsg: ${{ github.event.head_commit.message }}
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  acceptancetest_0:
    # The type of runner that the job will run on
    runs-on: ubuntu-24.04
    if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4
      - uses: ./.github/actions/checkout_mg5
      - uses: ./.github/actions/restore_heptools
      - uses: ./.github/actions/restore_all


      # Runs a set of commands using the runners shell
      - name: test one of the test test_hepmc_decay test_lhe_none_decay test_madspin_spin_only test_madspin_mixed_flavor_decay_log_summary test_madspin_wplus_all_all_flavor_balance test_madspin_wplus_all_all_flavor_balance_2to1
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_hepmc_decay -pA -t0 -l INFO
            ./tests/test_manager.py test_lhe_none_decay -pA -t0 -l INFO
            ./tests/test_manager.py test_madspin_spin_only -pA -t0 -l INFO
            ./tests/test_manager.py test_madspin_mixed_flavor_decay_log_summary -pA -t0 -l INFO
            ./tests/test_manager.py test_madspin_wplus_all_all_flavor_balance -pA -t0 -l INFO
            ./tests/test_manager.py test_madspin_wplus_all_all_flavor_balance_2to1 -pA -t0 -l INFO


  acceptancetest_madevent_bundle_1:
    # former jobs, run one after the other: acceptancetest_10 acceptancetest_50
    #   acceptancetest_contur acceptancetest_60
    #   acceptancetest_uncovered_madevent
    # Each keeps its own steps, run even when an earlier one failed.
    runs-on: ubuntu-24.04
    if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/checkout_mg5
      - uses: ./.github/actions/restore_heptools
      - uses: ./.github/actions/restore-pip-cache

      # ---- acceptancetest_10 ----
      - name: "acceptancetest_10"
        if: ${{ !cancelled() }}
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_DY_onejet -pA -t0 -l INFO

      # ---- acceptancetest_50 ----
      - name: "acceptancetest_50"
        if: ${{ !cancelled() }}
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_group_subprocess -pA -t0 -l INFO

      # ---- acceptancetest_contur ----
      - name: "acceptancetest_contur: run the test"
        if: ${{ !cancelled() }}
        run: |
            #./tests/test_manager.py test_contur_from_file test_rivet_from_file -pA -t0 -l INFO
            PYTHONPATH=$PYTHONPATH:/home/runner/.cache/HEPtools/contur/python3.10:/home/runner/.cache/HEPTools/rivet/local/lib/python3.10/dist-packages/:/home/runner/.cache/HEPTools/yoda/local/lib/python3.10/dist-packages/ LD_LIBRARY_PATH=$LD_LIBRARY_PATH:/home/runner/.cache/HEPTools/lib ./tests/test_manager.py  test_rivet_from_file -pA -t0 -l INFO

      # ---- acceptancetest_60 ----
      - name: "acceptancetest_60"
        if: ${{ !cancelled() }}
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_generation_from_file_1 -pA -t0 -l INFO

      # ---- acceptancetest_uncovered_madevent ----
      # Previously uncovered acceptance tests in tests/acceptance_tests/test_cmd_madevent.py
      # Includes test_madevent_dy3j_mlm after reopening/owning stdout safely.
      - name: "acceptancetest_uncovered_madevent: test madevent uncovered acceptance tests"
        if: ${{ !cancelled() }}
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_flavor_grouping_consistency_width test_madevent_dy3j_mlm -pA -t0 -l INFO


  acceptancetest_madevent_bundle_2:
    # former jobs, run one after the other: acceptancetest_17 acceptancetest_21
    #   acceptancetest_25 acceptancetest_35 acceptancetest_44 acceptancetest_55
    #   acceptancetest_59 acceptancetest_mssm_gogo
    # Each keeps its own steps, run even when an earlier one failed.
    runs-on: ubuntu-24.04
    if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/checkout_mg5
      - uses: ./.github/actions/restore_heptools
      - uses: ./.github/actions/restore-pip-cache

      # ---- acceptancetest_17 ----
      - name: "acceptancetest_17"
        if: ${{ !cancelled() }}
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_leshouche_sextet_diquarks -pA -t0 -l INFO

      # ---- acceptancetest_21 ----
      - name: "acceptancetest_21"
        if: ${{ !cancelled() }}
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_madevent_subproc_group_decay_chain -pA -t0 -l INFO

      # ---- acceptancetest_25 ----
      - name: "acceptancetest_25"
        if: ${{ !cancelled() }}
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_output_madevent_directory -pA -t0 -l INFO

      # ---- acceptancetest_35 ----
      - name: "acceptancetest_35"
        if: ${{ !cancelled() }}
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_ungroup_decay -pA -t0 -l INFO

      # ---- acceptancetest_44 ----
      - name: "acceptancetest_44"
        if: ${{ !cancelled() }}
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_creating_matched_plot -pA -t0 -l INFO

      # ---- acceptancetest_55 ----
      - name: "acceptancetest_55"
        if: ${{ !cancelled() }}
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_width_scan -pA -t0 -l INFO

      # ---- acceptancetest_59 ----
      - name: "acceptancetest_59"
        if: ${{ !cancelled() }}
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_decay_width_nlo_model -pA -t0 -l INFO

      # ---- acceptancetest_mssm_gogo ----
      # Fortran madevent supports MSSM p p > go go (merged-flavor squark/gluino
      # vertices); the mg7/madmatrix counterpart now also supports it and is
      # checked per-flavor (test_madmatrix_mssm_gogo in acceptancetest_mg7.yml).
      - name: "acceptancetest_mssm_gogo"
        if: ${{ !cancelled() }}
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_madevent_mssm_gogo -pA -t0 -l INFO


  acceptancetest_madevent_bundle_3:
    # former jobs, run one after the other: acceptancetest_19 acceptancetest_51
    #   acceptancetest_54 acceptancetest_uncovered_merged
    #   acceptancetest_density_wpwm acceptancetest_density_decay1
    #   acceptancetest_density_decay2
    # Each keeps its own steps, run even when an earlier one failed.
    runs-on: ubuntu-24.04
    if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/checkout_mg5
      - uses: ./.github/actions/restore_heptools
      - uses: ./.github/actions/restore-pip-cache

      # ---- acceptancetest_19 ----
      - name: "acceptancetest_19"
        if: ${{ !cancelled() }}
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_madevent_decay_chain -pA -t0 -l INFO

      # ---- acceptancetest_51 ----
      - name: "acceptancetest_51"
        if: ${{ !cancelled() }}
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_madevent_ptj_bias -pA -t0 -l INFO

      # ---- acceptancetest_54 ----
      - name: "acceptancetest_54"
        if: ${{ !cancelled() }}
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_width_nlocomputation -pA -t0 -l INFO

      # ---- acceptancetest_uncovered_merged ----
      # Previously uncovered acceptance tests in tests/acceptance_tests/test_cmd.py
      # related to the merged/ufo_aloha pipelines and decay-chain ordering
      - name: "acceptancetest_uncovered_merged: test cmd uncovered merged/ufo_aloha/standalone tests"
        if: ${{ !cancelled() }}
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_decay_chain_identical_particle_outoforder test_madevent_ufo_aloha_merged -pA -t0 -l INFO

      # ---- acceptancetest_density_wpwm ----
      - name: "acceptancetest_density_wpwm"
        if: ${{ !cancelled() }}
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_density_mode_wpwm -pA -t0 -l INFO

      # ---- acceptancetest_density_decay1 ----
      - name: "acceptancetest_density_decay1"
        if: ${{ !cancelled() }}
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_density_mode_decay1 -pA -t0 -l INFO

      # ---- acceptancetest_density_decay2 ----
      - name: "acceptancetest_density_decay2"
        if: ${{ !cancelled() }}
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_density_mode_decay2 -pA -t0 -l INFO


  acceptancetest_madevent_bundle_4:
    # former jobs, run one after the other: acceptancetest_20 acceptancetest_22
    #   acceptancetest_24 acceptancetest_38 acceptancetest_45 acceptancetest_47
    #   acceptancetest_56 acceptancetest_density_ttbar
    # Each keeps its own steps, run even when an earlier one failed.
    runs-on: ubuntu-24.04
    if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/checkout_mg5
      - uses: ./.github/actions/restore_heptools
      - uses: ./.github/actions/restore-pip-cache

      # ---- acceptancetest_20 ----
      - name: "acceptancetest_20"
        if: ${{ !cancelled() }}
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_madevent_subproc_group -pA -t0 -l INFO

      # ---- acceptancetest_22 ----
      - name: "acceptancetest_22"
        if: ${{ !cancelled() }}
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_madevent_subproc_group_symmetry -pA -t0 -l INFO

      # ---- acceptancetest_24 ----
      - name: "acceptancetest_24"
        if: ${{ !cancelled() }}
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_madevent_ufo_aloha -pA -t0 -l INFO

      # ---- acceptancetest_38 ----
      - name: "acceptancetest_38"
        if: ${{ !cancelled() }}
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_mass_reweighting -pA -t0 -l INFO

      # ---- acceptancetest_45 ----
      - name: "acceptancetest_45"
        if: ${{ !cancelled() }}
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_customised_madevent_via_run_card -pA -t0 -l INFO

      # ---- acceptancetest_47 ----
      - name: "acceptancetest_47"
        if: ${{ !cancelled() }}
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_e_p_collision -pA -t0 -l INFO

      # ---- acceptancetest_56 ----
      - name: "acceptancetest_56"
        if: ${{ !cancelled() }}
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_run_fromP -pA -t0 -l INFO

      # ---- acceptancetest_density_ttbar ----
      - name: "acceptancetest_density_ttbar"
        if: ${{ !cancelled() }}
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_density_mode_ttbar -pA -t0 -l DEBUG


  acceptancetest_madevent_bundle_5:
    # former jobs, run one after the other: acceptancetest_37 acceptancetest_52
    #   acceptancetest_density_interface
    # Each keeps its own steps, run even when an earlier one failed.
    runs-on: ubuntu-24.04
    if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/checkout_mg5
      - uses: ./.github/actions/restore-pip-cache
      - uses: ./.github/actions/restore_heptools

      # ---- acceptancetest_37 ----
      - name: "acceptancetest_37"
        if: ${{ !cancelled() }}
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py  test_w_production_with_ms_decay test_add_time_of_flight -pA -t0 -l INFO

      # ---- acceptancetest_52 ----
      - name: "acceptancetest_52"
        if: ${{ !cancelled() }}
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_madspin_gridpack -pA -t0 -l INFO

      # ---- acceptancetest_density_interface ----
      - name: "acceptancetest_density_interface"
        if: ${{ !cancelled() }}
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_density_mode_user_interface -pA -t0 -l INFO


  test_oneloop_reweighting:
    # The type of runner that the job will run on
    runs-on: ${{ matrix.os }}
    if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true
    strategy:
      matrix:
        os: [ubuntu-24.04]

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4
      - uses: ./.github/actions/checkout_mg5
      - uses: ./.github/actions/restore_heptools
      - uses: ./.github/actions/restore-pip-cache
      - uses: ./.github/actions/restore_all

      - name: Install Meson and Ninja
        run: |
           sudo apt-get update
           sudo apt-get install -y meson ninja-build


      # Runs a set of commands using the runners shell
      - name: test one of the test test_oneloop_reweighting
        run: |
            cd $GITHUB_WORKSPACE
      # Runs a set of commands using the runners shell
      - name: test one of the test test_oneloop_reweighting
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_oneloop_reweighting -pA -t0 -l INFO


  acceptancetest_madevent_bundle_6:
    # former jobs, run one after the other: acceptancetest_42 acceptancetest_43
    #   acceptancetest_46 acceptancetest_49 acceptancetest_contur2
    #   acceptancetest_density_tttbartbar
    # Each keeps its own steps, run even when an earlier one failed.
    runs-on: ubuntu-24.04
    if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/checkout_mg5
      - uses: ./.github/actions/restore_heptools
      - uses: ./.github/actions/restore-pip-cache

      # ---- acceptancetest_42 ----
      - name: "acceptancetest_42"
        if: ${{ !cancelled() }}
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_scan_reweighting -pA -t0 -l INFO

      # ---- acceptancetest_43 ----
      - name: "acceptancetest_43"
        if: ${{ !cancelled() }}
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_complex_mass_scheme -pA -t0 -l INFO

      # ---- acceptancetest_46 ----
      - name: "acceptancetest_46"
        if: ${{ !cancelled() }}
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_e_e_collision -pA -t0 -l INFO

      # ---- acceptancetest_49 ----
      - name: "acceptancetest_49"
        if: ${{ !cancelled() }}
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_eva_collision test_eva_oldrelease_collision test_ieva_collision -pA -t0 -l INFO

      # ---- acceptancetest_contur2 ----
      - name: "acceptancetest_contur2: run the test"
        if: ${{ !cancelled() }}
        run: |
            PYTHONPATH=$PYTHONPATH:/home/runner/.cache/HEPtools/contur/python3.10 LD_LIBRARY_PATH=$LD_LIBRARY_PATH:/home/runner/.cache/HEPTools/lib ./tests/test_manager.py test_rivet_from_file -pA -t0 -l INFO

      # ---- acceptancetest_density_tttbartbar ----
      - name: "acceptancetest_density_tttbartbar"
        if: ${{ !cancelled() }}
        run: |
            cd $GITHUB_WORKSPACE
            # NB: no 'cp input/.mg7_configuration_default.txt' here -- it would
            # run after restore_heptools and wipe the lhapdf paths it wrote,
            # leaving the LO default (pdlabel=lhapdf) with no lhapdf-config.
            # checkout_mg5 already resets the file, before restore_heptools.
            ./tests/test_manager.py test_density_mode_doublettbar -pA -t0 -l INFO


  acceptancetest_madevent_bundle_7:
    # former jobs, run one after the other: acceptancetest_48
    #   acceptancetest_heft acceptancetest_madevent_flavor_zud
    # Each keeps its own steps, run even when an earlier one failed.
    runs-on: ubuntu-24.04
    if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/checkout_mg5
      - uses: ./.github/actions/restore_heptools

      # ---- acceptancetest_48 ----
      - name: "acceptancetest_48"
        if: ${{ !cancelled() }}
        run: |
            cd $GITHUB_WORKSPACE
            echo "install RunningCoupling" > cmd
            ./bin/madgraph cmd
            ./tests/test_manager.py test_eft_running -pA -t0 -l INFO

      # ---- acceptancetest_heft ----
      - name: "acceptancetest_heft"
        if: ${{ !cancelled() }}
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_generation_heft -pA -t0 -l INFO

      # ---- acceptancetest_madevent_flavor_zud ----
      # Flavor-grouped u q > Z u q QCD=0 tests (grouped and ungrouped).
      - name: "acceptancetest_madevent_flavor_zud: test madevent flavor zud (grouped and nogroup)"
        if: ${{ !cancelled() }}
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_madevent_flavor_zud test_madevent_flavor_zud_nogroup -pA -t0 -l INFO


  acceptancetest_53:
    # The type of runner that the job will run on
    runs-on: ubuntu-24.04
    if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4
      - uses: ./.github/actions/checkout_mg5
      - uses: ./.github/actions/restore_heptools
      - uses: ./.github/actions/restore-pip-cache
      - uses: ./.github/actions/restore_ufomodel

      # Runs a set of commands using the runners shell
      - name: test one of the test test_width_computation
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_width_computation -pA -t0 -l INFO

  acceptancetest_102:
    # The type of runner that the job will run on

    runs-on: ${{ matrix.os }}
    if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true
    strategy:
      matrix:
        os: [ubuntu-24.04]

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4
      - uses: ./.github/actions/checkout_mg5
      - uses: ./.github/actions/restore-pip-cache
      - uses: ./.github/actions/restore_heptools
      - name: Install Meson and Ninja
        run: |
           sudo apt-get update
           sudo apt-get install -y meson ninja-build

      # Runs a set of commands using the runners shell
      - name: test one of the test test_loop_improved_reweighting
        run: |
            cd $GITHUB_WORKSPACE
            export LD_LIBRARY_PATH=$LD_LIBRARY_PATH:/home/runner/.cache/HEPtools/lhapdf6_py3/lib
            export PYTHONPATH=$PYTHONPATH:~/.cache/HEPtools/lhapdf6_py3/lib/python3.12/dist-packages
            export PYTHONPATH=$PYTHONPATH:~/.cache/HEPtools/lhapdf6_py3/lib/python3.10/dist-packages
            ./tests/test_manager.py  test_nlo_reweighting -pA -t0 -l INFO


  acceptancetest_madevent_bundle_8:
    # former jobs, run one after the other: acceptancetest_103_polarization
    #   acceptancetest_flavor acceptancetest_madevent_vbf_merged_flavor
    # Each keeps its own steps, run even when an earlier one failed.
    runs-on: ubuntu-24.04
    if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == true
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/checkout_mg5
      - uses: ./.github/actions/restore_heptools

      # ---- acceptancetest_103_polarization ----
      - name: "acceptancetest_103_polarization"
        if: ${{ !cancelled() }}
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_polarization_top_decay -pA -t0 -l INFO

      # ---- acceptancetest_flavor ----
      - name: "acceptancetest_flavor: test flavor consistency and decay-chain symmetry factor"
        if: ${{ !cancelled() }}
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_flavor_grouping_consistency_mlm test_flavor_grouping_consistency test_decay_chain_symmetry_factor -pA -t0 -l INFO

      # ---- acceptancetest_madevent_vbf_merged_flavor ----
      # Previously uncovered acceptance test in tests/acceptance_tests/test_cmd_madloop.py
      - name: "acceptancetest_madevent_vbf_merged_flavor: test madevent_with_merged_flavor"
        if: ${{ !cancelled() }}
        run: |
            cd $GITHUB_WORKSPACE
            ./tests/test_manager.py test_madevent_merged_flavor_uq test_madevent_flavor_zjj -pA -t0 -l INFO

